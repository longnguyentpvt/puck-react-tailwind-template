# External Data Configuration
# Copy this file to .env.local and update with your values

# Site URL for server-side rendering (required for external data fetching)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
